# Plano de Remoção de Código Morto/Inútil

## Objetivo

Realizar uma varredura completa no código-fonte e remover código morto, importações não utilizadas, arquivos órfãos e dependências não utilizadas.

---

## 1. Arquivos e Componentes Não Utilizados

| Arquivo                                                  | Ação        | Razão                                                                                                      |
| -------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| `src/lib/components/Spinner.svelte`                      | **REMOVER** | Criado mas nunca importado. O LoadingOverlay usa um spinner inline (comentado como "Spinner" na linha 24). |
| `src/lib/index.ts`                                       | **REMOVER** | Arquivo placeholder vazio com apenas 1 linha de comentário.                                                |
| `src/lib/components/PainelAssinaturaToken.legacy.svelte` | **REMOVER** | Arquivo legacy não referenciado em nenhum lugar.                                                           |

---

## 2. ~~Imports Não Utilizados~~ (CORRIGIDO: Todos estão sendo usados)

| Arquivo                                | Linha | Import       | Status                      |
| -------------------------------------- | ----- | ------------ | --------------------------- |
| `src/lib/export.ts`                    | 15    | `proximoDia` | **USADO** - linha 34        |
| `src/routes/escalas/[id]/+page.svelte` | 9     | `proximoDia` | **USADO** - linhas 119, 126 |

Todos os imports de `proximoDia` estão sendo utilizados. O агente inicial cometeu um erro de análise.

## 3. Funções Exportadas mas Não Utilizadas

| Arquivo                                                | Função/Variável          | Ação                                                                                                        |
| ------------------------------------------------------ | ------------------------ | ----------------------------------------------------------------------------------------------------------- |
| `src/lib/composables/useLocalStorageFilters.svelte.ts` | `createLocalStorageSync` | **REMOVER da exportação** em `src/lib/composables/index.ts` - exportado mas nunca importado em nenhum lugar |

---

## 4. Código Duplicado (Reorganização Recomendada)

### `formatarData` - 4+ implementações diferentes:

1. **`src/lib/utils.ts`** - `formatarData(dateStr: string): string` (principal)
2. **`src/lib/export.ts`** - `formatarDataPlantao(p, escala): string` (usa a principal + lógica adicional)
3. **`src/routes/escalas/[id]/+page.svelte`** - `formatarDataPlantao(p): string` (duplicada localmente)
4. **`src/routes/validar/[hash]/+page.svelte`** - `formatarDataHora(dateStr)` (diferente - formata data+hora)

**Recomendação:** Manter apenas a versão principal em `utils.ts` e as variações específicas onde são necessárias (export.ts e validar).

---

## 5. Dependências de Pacotes Potencialmente Não Utilizadas

### Verificar antes de remover:

| Pacote      | Status Atual                                        | Ação Recomendada            |
| ----------- | --------------------------------------------------- | --------------------------- |
| `dompurify` | **NÃO USADO** - nenhuma referência no código        | **REMOVER do package.json** |
| `web-pki`   | Usado em `src/lib/webpki.ts` linha 36 - **MANTIDO** | -                           |

---

## 6. Código Comentado/Type Augmentation

| Arquivo             | Linha | Conteúdo                                                                                  |
| ------------------- | ----- | ----------------------------------------------------------------------------------------- |
| `src/lib/export.ts` | 9-12  | Type augmentation para jspdf-autotable - **MANTIDO** (necessário para o plugin funcionar) |

---

## Resumo das Ações de Remoção

### Arquivos para remover:

1. `src/lib/components/Spinner.svelte`
2. `src/lib/index.ts`
3. ~~`src/lib/components/PainelAssinaturaToken.legacy.svelte`~~ (não existe)

### Código para remover:

1. `createLocalStorageSync` das exports em `src/lib/composables/index.ts` (linha 5)

### Dependências para remover do package.json:

1. `dompurify` - não está sendo usado em nenhum lugar do código

---

## 7. Scripts da pasta `/scripts`

### Análise dos scripts existentes:

| Script                     | Uso                                                 | Status                              |
| -------------------------- | --------------------------------------------------- | ----------------------------------- |
| `migrate.ts`               | Migrações Drizzle/Cloudflare (`npm run db:migrate`) | **MANTIDO** - usado ativamente      |
| `reset-admin-password.ts`  | Reset de senha admin via CLI                        | **MANTIDO** - utilitário útil       |
| `renumber-migrations.ps1`  | Renomear migrations sequencialmente                 | **MANTIDO** - utilitário útil       |
| `GoogleAppsScript_Sync.gs` | Sincronização Google Sheets → API                   | **MANTIDO** - usado para integração |
| `update-page.cjs`          | One-time: atualiza +page.svelte de escalas          | **APAGAR** - executado uma vez      |
| `update-export.cjs`        | One-time: adiciona funções em export.ts             | **APAGAR** - executado uma vez      |
| `update-proximo-mes.cjs`   | One-time: atualiza API de próximo mês               | **APAGAR** - executado uma vez      |
| `update-api.cjs`           | One-time: atualiza API de policiais                 | **APAGAR** - executado uma vez      |

### Scripts para remover:

1. `scripts/update-page.cjs`
2. `scripts/update-export.cjs`
3. `scripts/update-proximo-mes.cjs`
4. `scripts/update-api.cjs`

---

## Resumo Final das Remoções

### Arquivos para remover:

1. `src/lib/components/Spinner.svelte`
2. `src/lib/index.ts`
3. `scripts/update-page.cjs`
4. `scripts/update-export.cjs`
5. `scripts/update-proximo-mes.cjs`
6. `scripts/update-api.cjs`

### Código para remover:

1. `createLocalStorageSync` das exports em `src/lib/composables/index.ts` (linha 5)

### Dependências para remover do package.json:

1. `dompurify`

### Verificações pendentes:

1. ~~`dompurify`~~ - CONFIRMADO: não é usado, pode remover

---

## Execução

O executor deve:

1. Verificar se os arquivos realmente existem antes de remover
2. Rodar `npm run check` após alterações para garantir que nada quebrou
3. Remover dependências do package.json apenas após confirmar que não são usadas
